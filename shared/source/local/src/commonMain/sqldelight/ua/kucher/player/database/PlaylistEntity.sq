CREATE TABLE IF NOT EXISTS PlaylistEntity (
 id INTEGER PRIMARY KEY NOT NULL,
 name TEXT NOT NULL
);

CREATE INDEX IF NOT EXISTS playlist_name ON PlaylistEntity(name);

insertFavouritePlaylist:
INSERT OR IGNORE INTO PlaylistEntity(id, name)
VALUES (1, 'Favorite');